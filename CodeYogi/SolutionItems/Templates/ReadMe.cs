//Read me!

Assumptions:

1. Every table must have primary key for consistant output
2. Database design meets the data integrity rule


Procedures:
1. Based on table columns data crud operations are handled.
2. Primary key column is removed from insert and update list.
3. Every procedure have default and paging parameters.
4. For import xmldata is used and for single record normal parameter  values inserted into temp table and managed crud operatio by doing merge.
5. 
